# 概念书
## todos
1. 可以clash+git拉代码，存到本地存储空间，然后进行devops的docker build 和push 到harbor，这些可以统一配置，集成配置中心。
2. 可以docker镜像配置测速，支持docker镜像和代理设置等。
3. 可以进行爬虫的工作流编写，支持简单爬虫任务。
4. 前端可以完全的ui管理
5. 支持agent对话协助执行命令，支持mcp和skill的管理、自主安装、使用。
6. 支持agent对话主动修改代码，包括自己本身，支持git的规范化版本管理，会自主部署。
7. 支持服务器的各项指标展示查看。支持分布式的部署（k8s）和直接的k8s调度管理面板。
8. 支持任务调度的概念，可以由agent作为入口异步执行爬虫、分析报表、自动签到等自动化独立任务。

## 日志
### 3.5
1. 添加infra。shell和store，job。
2. workflow模块编写。

### 3.7
1. 构建agent能力和skills基础能力。
2. mihomo代理功能接入。


### 3.12
1. 短记忆，记忆压缩
2. 部署相关
3. agent对话优化：流式、样式
4. 飞书client能力接入。
5. 脚本模块安全性优化。
6. 部署便捷性优化。
7. 添加默认skills